# 2026-08-07 Devlog

## 今日摘要

今天改善病患結構化問診的可修正性，讓使用者能取消已選選項、以「其他」文字取代
舊選項，並回到上一題重新作答；傳統前端與拆分後的 patient app 維持一致行為。
醫師端則補上無目前就診情境的後臺模式、邀請能力提示，以及 UCC session 失效判斷，
避免登入頁 HTML 或失效回應被誤當成有效 bootstrap。同步更新 OpenAPI 契約、產生型別、
開發待辦與本機部署暫存檔忽略規則。

## 病患問卷操作

- 病患 chat request 新增 `answer`／`back` action；後端在每次有效作答前保存有限長度的
  session 快照，可還原上一題、問卷資料與 AMIE transcript 長度，同時保留整合授權內容。
- chat response 新增 `can_go_back`，兩套病患前端依後端狀態顯示「回到上一題」，返回後
  移除畫面上最後一輪作答與對應 trace，讓病患可重新輸入。
- 單選與複選選項皆可再次點擊取消；輸入「其他」時會清除舊選項，重新點選選項也會
  清除「其他」文字，避免送出互相衝突的答案。
- 放大 body pain map 左右方向標示並提高字重與字距，改善定位辨識。
- 新增後端上一題狀態還原測試，以及舊版與拆分前端的選項取消／其他文字測試。

## 醫師後臺與 UCC 工作階段

- 醫師 dashboard 可在沒有 `regSno`／encounter 時啟動，保留病例查詢、醫師助手與規則
  中心；患者邀請按鈕維持可見但停用，並顯示需要先從 B.看診作業開啟就診的提示。
- 畫面新增目前就診狀態列，清楚區分一般醫師後臺與綁定 encounter 的邀請模式。
- invitation capability 同時檢查 bootstrap 能力、CSRF token 與 encounter 的 `regSno`，
  不再只依瀏覽器 token scope；送出邀請前會再次驗證目前就診一致性。
- bootstrap 將 redirect、401／403、非 JSON content type 與破損 JSON 統一辨識為 UCC
  session 失效；有效 JSON 但缺少 access token 仍保留獨立錯誤訊息。
- 新增 doctor session 回歸測試及 eHIS dashboard／invitation source contract 測試。

## API 契約與開發維護

- 重新產生 `docs/openapi.json` 與舊前端 TypeScript API definitions，納入近期安全端點、
  invitation／patient session schema，以及本次 `action`、`can_go_back` 欄位。
- README 新增近期開發項目，記錄語音模型、問卷互動、疼痛位置呈現與 UCC 實機測試方向。
- 忽略 `integration-deployment/staging/`，避免 IIS ARR／URL Rewrite 安裝包、安裝紀錄與
  rollback 暫存資料誤入版控；本機既有 staging 檔案未納入 commit。

## 驗證

- 舊版前端 Node 測試：65 項通過。
- `frontend-v2` Node 測試：21 項通過。
- 後端上一題狀態還原測試：1 項通過。
- 舊版前端與 `frontend-v2` doctor／patient production build：通過。
- OpenAPI 產生型別同步檢查：通過。
- OpenAPI committed contract 與目前 FastAPI application 一致性測試：通過。
- 各批 commit 前的 `git diff --cached --check`：通過。

另執行 OpenAPI、病患 triage、問卷與上一題相關的 32 項後端測試，其中 29 項通過；
既有 OpenAPI 測試仍有 2 項失敗、1 項錯誤：預期端點清單尚未納入 invitation／patient
session 路由，且兩個直接呼叫 doctor route 的案例未注入新的 UCC authentication
principal。這些問題不影響本次上一題測試，但需要後續更新測試 fixture 與端點預期。

## 後續工作

- 更新 `test_openapi_contract.py` 的版本化端點清單，並為 doctor route 測試注入已驗證的
  UCC principal，使安全邊界後的完整契約測試恢復全綠。
- 在實際 UCC 登入逾時、無 encounter dashboard 與 B.看診作業 iframe 流程進行端到端
  驗收，確認狀態提示、重新登入與 invitation capability 行為。
- 為連續多次返回、AMIE 問診與 patient HttpOnly session 流程補上整合測試。

## 今日 commits

- `a15b092` — `feat: improve patient questionnaire controls`
- `ed7f370` — `feat: support encounter-free doctor dashboard`
- `ecd9a04` — `docs: sync OpenAPI contract and generated types`
- `e0bc59a` — `chore: update backlog and staging ignores`
