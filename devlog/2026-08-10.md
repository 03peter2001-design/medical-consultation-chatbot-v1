# 2026-08-10

## 本機 backend 使用 Docker GPU Avatar

- Integration deployment bundle 由 `1.0.0` 升至 `1.1.0`。
- Avatar service 新增固定的 host loopback mapping：`127.0.0.1:8090`。本機執行的
  FastAPI backend 可透過此位址呼叫 CosyVoice3／MuseTalk，而不需要將 backend 或
  development frontend 容器化。
- 模型端點仍不對 LAN 或公網發布；正式瀏覽器流程維持 gateway → authenticated
  backend → private Avatar service 的邊界。
- 本機未提交的 `backend/.env` 啟用 Avatar 並指向 loopback；部署端可用
  `AVATAR_HOST_PORT` 避免本機 port 衝突，但綁定介面固定為 loopback。

### 驗證

- `docker compose config --quiet`
- Avatar health 必須回報 `status=ok`、`device=cuda:0`、`models_downloaded=true`
- Docker backend 必須能經 `http://avatar:8090` 取得 health
- Host 必須能經 `http://127.0.0.1:8090/health` 取得相同狀態
- 以不含病例資料的合成短句從本機 backend 呼叫 Avatar，成功取得
  `video/mp4`（89,888 bytes；首次 cache miss）

## 開發版 frontend 固定連 Docker backend

- Integration deployment bundle 由 `1.1.0` 升至 `1.2.0`。
- Gateway 新增 host-loopback-only 的 port 18000 mapping（container listener 8000）；`frontend/` 維持 Vite
  開發模式，但 API、問診、病例、Breeze ASR 與 Avatar 全部由 Compose services
  提供。FastAPI 的 container port 沒有直接發布。
- 本機 deployment `.env` 明確開啟 loopback auth bypass 與兩個 Vite origin 的 CORS；
  `.env.example` 維持 bypass 關閉，避免正式環境沿用開發授權。
- Patient／UCC TLS listeners 改為覆寫 `X-Forwarded-For`，不接受用戶注入的 forwarded
  chain；只有 loopback-published development listener 將來源標記為 `127.0.0.1`。
- 修正 gateway 只加入 internal network、導致宣告的 443／8443／development port
  實際未發布的部署缺陷；新增 gateway-only edge bridge，backend 仍不發布 host port。
- Patient／UCC host ports 改為可設定，正式預設維持 443／8443；本機以 10443 避開
  已存在的 443 listener，development API 仍固定使用 loopback 18000。
- Backend 原始碼更新後執行 `docker compose up -d --build backend`；frontend 修改仍由
  Vite hot reload。此流程使用 Docker `consultation-data` volume，不會自動搬移本機
  SQLite 病例。
- Nginx upstream 使用 Docker DNS 動態重新解析 `backend:8000`，避免只重建 backend
  後仍保留舊 container IP 而產生 502。
- Development frontend 由 `1.2.0` 升至 `1.3.0`，預設 API port 改為 18000，避免與
  仍在 host port 8000 的 Uvicorn process 發生路由混淆。

### 驗證

- `docker compose config --quiet` 與 `git diff --check` 通過。
- `docker compose up -d --build` 成功建置並啟動 backend、Avatar 與 gateway；三者
  最終皆為 healthy。
- 實際 Docker mapping 為 `127.0.0.1:18000 → gateway:8000`、
  `127.0.0.1:8090 → avatar:8090`；FastAPI 只有 container port。
- `http://localhost:18000/v1/health` 回應 Docker backend；醫師病例開發白名單可用，
  且只記錄筆數、不在 devlog 保存病例內容。
- localhost:5173 CORS preflight 回 200 並只回允許的 origin；Avatar status 回報
  `enabled=true`、`available=true`、`device=cuda:0`。
- 對 public TLS listener 偽造 `X-Forwarded-For: 127.0.0.1` 仍回 401。
- `docker compose up -d --build backend` 單獨重建後，未重啟的 gateway 可經 Docker
  DNS 解析新 backend，Avatar status 仍成功。
- Frontend 13 項 Node tests、Vite production build、OpenAPI type drift check 通過；
  Nginx `nginx -t` 通過。Ubuntu 未安裝 `pwsh`，因此未直接執行 PowerShell static
  validator；其新增 invariants 已由實際 Compose/Nginx/HTTP checks 覆蓋。

## Avatar 中央舞台與語音優先問診

- Development frontend 由 `1.3.0` 升至 `1.4.0`；未同步修改獨立發布線
  `frontend-v2/`。
- 修正 Vite 直接啟動時引用不存在 `/avatar/doctor.png` 的破圖，改由 Vite public
  assets 帶入既有合成醫師圖片；Docker gateway 與 Vite 皆可使用相同畫面資產。
- Avatar 啟用後，在問診畫面中央上方顯示醫師影像，右側顯示目前朗讀字幕，
  下方聊天與文字輸入仍可使用。設定抽屜的預覽改為靜音，避免與中央舞台重複播音。
- Avatar 播放完成後，文字題自動開始錄音；只有偵測到使用者實際說話後才開始
  計算停頓，連續靜音 5 秒會停止並送交 Breeze ASR。
- 辨識結果回填可編輯輸入框，3 秒後自動送出；開始編輯會取消 timer，手動停止
  錄音也維持人工確認後送出的既有流程。

### 驗證

- `npm test`：13 個 Node test files 全數通過，新增語音活動 RMS、說話前不誤停、
  5 秒邊界、自動送出可取消，以及中央字幕舞台 regression coverage。
- `npm run build` 與 `npm run api:check` 通過；production dist 包含醫師 PNG。
- 主機 loopback 實測 Vite 圖片回 200；Avatar API 回報
  `enabled=true`、`available=true`、`device=cuda:0`。
- 使用 Playwright 1.62.1 搭配系統 Chrome 實測 1440×960 與 390×844 viewport：
  醫師圖片完整載入、字幕與底部輸入可見、GPU Avatar 播放後自動進入錄音，
  且無 page runtime error。

## Avatar 雙語、無縫嘴型與模型常駐

- Backend API 由 `1.3.0` 升至 `1.4.0`、development frontend 由 `1.4.0`
  升至 `1.5.0`、Local Avatar service 由 `1.0.0` 升至 `1.1.0`、Integration
  deployment bundle 由 `1.2.0` 升至 `1.3.0`；未修改 `frontend-v2/`。
- Avatar 合成契約新增向後相容的 `mandarin`／`minnan` enum。前端設定可選國語
  或閩南語，CosyVoice3 使用各自 instruction，cache key 也包含語言，避免跨語言
  使用錯誤影片。官方模型卡明列支援 Minnan dialect 與 instruction control。
- 修正自訂 MuseTalk blending 把整個下半臉矩形貼回原圖的缺陷；新遮罩只融合
  嘴部／下半臉中央並在四周羽化至零，因此保留臉部外緣、下巴、頸部與衣領原圖。
- 新增受病患 session 保護的 `/v1/avatar/warmup`；啟用 Avatar 時依序載入 Breeze
  ASR、CosyVoice3、MuseTalk、VAE 與嘴型音訊 encoder，所有模型成功後才回報啟用。
  Integration 16 GB profile 預設不再於每次辨識／合成後卸載。
- ASR 回傳空內容、解碼失敗或推論失敗時，前端不送出病人回答；已連線醫生改為
  說「對不起，我沒有聽清楚，請再講一次」，播放後重新開始語音輸入。

### 驗證

- 第一次 GPU warm-up 為 47.35 秒，第二次為 0.005 秒；Breeze、CosyVoice 與
  MuseTalk health 持續回報 loaded。
- 國語未快取合成成功（94,813 bytes，10.95 秒），其後閩南語合成成功
  （94,057 bytes，4.35 秒）；完成後常駐顯存約 11.5 GB，RTX 5060 Ti 尚餘
  約 4.3 GB。
- 新舊影片各擷取同時間點幀檢查；舊片下巴／衣領有清楚的矩形水平接縫，新片
  邊界已消失。單元測試另鎖定 blend crop 的上、下、左、右邊界完全使用原圖。
- Backend 聚焦 tests、Ruff、OpenAPI export／drift，Avatar service tests、frontend
  Node tests／production build／API type check 與 Compose／GPU 端到端驗證通過。
- Backend 全套共 358 項，356 項通過；2 項仍因未掛載外部
  `D:\ehis\eHIS` 原始碼而 error。本次 Python 檔案 Ruff 通過；repository-wide Ruff
  仍被既有未追蹤 `backend/scripts/amie_route_draft_schema.py` 的 import order 擋住。
  `pyright` 與 `lint-imports` 不在既有 venv，未另行安裝。

## CosyVoice 閩南語控制詞修正

- Local Avatar service 由 `1.1.0` 升至 `1.1.1`。
- 確認固定版 CosyVoice3 原始碼的內建閩南語 instruction 為
  `You are a helpful assistant. 请用闽南话表达。<|endofprompt|>`；先前自訂的繁體
  醫療語氣長句不屬於模型內建控制詞，實際推論可能被忽略而維持國語。
- 預設值改用官方精確控制詞，並加入 regression test。既有 cache key 已包含完整
  instruction，因此部署更新後會自動產生新影片，不會命中先前的錯誤快取。
- 本次只控制 CosyVoice 方言發音，沒有增加外部翻譯模型或改寫醫療文字；畫面字幕
  仍保留後端原始回覆。

## Avatar 載入狀態與麥克風收音波形

- Development frontend 由 `1.5.0` 升至 `1.6.0`；未修改 `frontend-v2/`。
- 本地 Avatar 初次啟用及每段影片生成時，設定抽屜的醫師預覽會顯示 loading overlay、
  spinner、目前模型階段與第一次載入說明；中央醫師舞台也會在 connecting 期間顯示，
  即使尚未產生字幕亦不再是一片空白。
- 沿用錄音流程既有的 Web Audio analyser，每 100 ms 將 RMS 正規化成七段即時波形；
  音量達語音閾值後顯示「已收到聲音」，讓病患確認麥克風確實收到說話內容。
- analyser 改為手動及 Avatar 自動錄音都啟用，視覺回饋一致；`autoStop` 狀態獨立，
  因此手動錄音不會意外套用 Avatar 的五秒靜音自動停止。

## 病患問診固定文案 JSON 化

- Backend API 由 `1.4.0` 升至 `1.5.0`。
- 新增 `questionnaire_data/ui/patient_messages.json`，集中 AMIE／legacy 開場、上一題、
  validation retry、section 轉場、一般／urgent 完成、人工 handoff 與流程報告模板；
  `app/routes/patient.py` 的 `_question_payload(reply=...)` 不再接收 inline 字串。
- 新增 `domain.patient_messages` loader，固定 schema version 1、`zh-TW` locale、完整
  key 集合及每個 template 可使用的 placeholder。缺漏 key、未知 key、placeholder
  改名或呼叫端參數漂移皆 fail closed。
- 輸入驗證與 `URGENT_CARE_MESSAGE` 也改由同一文案 catalog 取得；Safety 規則、LLM
  system prompt、audit reason、HTTP error 與其他非病患朗讀內容維持原有責任邊界。

### 驗證

- 43 項 patient-message／questionnaire／validation／back-navigation／triage 聚焦測試通過；
  本次 6 個 Python 檔案 Ruff check 與 format check 通過，`git diff --check` 通過。
- 完整 backend suite 共 369 項，367 項通過；2 項只因未掛載外部
  `D:\ehis\eHIS` C# 原始碼而 error。Repository-wide Ruff check 通過；format check
  仍指出 5 個非本次文案範圍的既有檔案。
- Docker backend 重建後為 healthy；透過 loopback gateway 送出不含病例資料的合成
  初始 session，回覆成功由 `interview.amie_welcome` 與 `chief.json` prompt 組裝。

## 疾病名稱導向的 provisional AMIE clinical artifacts

- Backend API 由 `1.5.0` 升至 `1.6.0`；病患 API、OpenAPI、active questionnaire、
  active Safety 與 active disease profiles 均未改變，runtime 仍只有胸痛、頭痛、腹痛。
- 新增可續跑的兩階段 Gemini＋RAG pipeline。第一階段依 route 問卷與 RAG 提出標準
  英文疾病名稱；第二階段逐疾病名稱查 RAG，再逐疾病產生 fact、exact-choice semantics、
  profile 與 Safety candidates。所有 chunks 保留 query、scope、hash 與 unverified citation。
- Local compiler 僅執行可稽核的機械修補：把缺漏 citation 限縮回該疾病完整 evidence
  pack、同步 namespace code 與引用、移除無 exact-choice option 可達的 proposed fact；
  不創造臨床條件、權重、診斷或緊急規則。
- v1–v7 用於 prompt／schema pilot 與失敗實驗；原始 pilot 目錄不納入版本控制，只保留
  此處的失敗結論。正式 v8 run 產生 49/49 route bundles，
  共 163 個疾病候選、431 facts、344 semantic mappings、118 profiles 與 82 Safety
  candidates。Manifest 仍列 45 個 profile 缺口、16 條無 Safety、6 條 evidence
  insufficient、8 個 critical notes、128 次 citation evidence-pack 修補與 137 次
  不可達 fact 裁切，因此 `clinical_review_ready=false`、`runtime_eligible=false`。
- `stab_wound` 在多次 Gemini schema repair 後仍失敗，最終只寫入保持 urgent 的
  fail-closed insufficient stub，沒有接受任何模型 Safety／fact；不得解讀為完成臨床內容。

### 驗證

- 7 項新單元測試涵蓋疾病名稱導向檢索、source scope、runtime 隔離、citation 修補、
  不可達 fact 裁切、code reference 同步及 urgent stub；聚焦 Ruff／format 通過。
- 131 項 AMIE／問卷聚焦測試全數通過；完整 backend suite 共 370 項、368 項通過，
  2 項只因本機未掛載外部 `D:\ehis\eHIS` C# 原始碼而 error。OpenAPI drift、兩份
  frontend questionnaire metadata drift、repository-wide Ruff、diff check 與 secrets
  掃描通過。
- 單一 reducer 驗證 49/49 bundle、全域 code 唯一性、跨 artifact 引用、檔案 hash 與
  blocking summary，輸出 `complete=true` 但明確維持 `clinical_review_ready=false`。

## 結構化問卷語音輸入

- Development frontend 由 `1.6.0` 升至 `1.7.0`；未修改獨立控制的
  `frontend-v2/` 正式部署線，亦未變更 backend API 或臨床規則。
- 將 Breeze ASR 延伸到 choice、multi-choice、duration 與 date 題型；結構化題顯示
  麥克風、既有 RMS 波形及辨識結果，Avatar 問完每一題後都可進入五秒靜音停止流程。
- 新增純前端的保守 mapper：只接受 exact option、可無歧義分割的 exact 多選、正數加
  核准單位，以及有效且不超過 130 歲的西元／民國日期。無法對應時只在題目允許時
  填入「其他／補充說明」，否則明確要求手動作答，不推測醫療語意。
- 結構化答案不沿用文字題的三秒自動送出；ASR 只預填既有控制項，仍需病人按原本的
  確認鍵，避免誤辨直接成為病歷答案。

### 驗證

- `npm test`：14 個 Node test files 全數通過；新增 exact single／multi choice、exclusive
  衝突、other fallback、duration、Gregorian／ROC date，以及無效／未來／超齡日期測試。
- `npm run build` 與 `npm run api:check` 通過。
- Browser plugin 與專案 Playwright 套件不可用，因此以系統 Chrome headless/CDP 驗證
  `http://127.0.0.1:5174/ai-consult/#/` 的合成基本資料選擇題；1440×960 與 390×844
  皆顯示語音列、收音波形及停止按鈕，互動後進入「正在聆聽您的答案」，無 console
  error／warning、空白頁或 Vite error overlay。

## Docker backend RAG writable Chroma mount

- Integration deployment bundle 由 `1.3.0` 升至 `1.3.1`。
- 修正 `chroma_db` 以唯讀 bind mount 提供給 backend，導致 Chroma
  `PersistentClient` 在 SQLite bookkeeping 階段回報 `attempt to write a readonly
  database`，並由 runtime fallback 停用 RAG 的部署缺陷。
- 掛載改為可寫後仍使用既有 host index 作為持久化來源；static validator 新增 invariant，
  防止 compose 再次將 `/app/chroma_db` 設為唯讀。
- 本次不變更 RAG corpus、collections、embedding model、臨床內容或病患問診決策。

### 驗證

- `docker compose config --quiet` 與 resolved Compose JSON 的 writable mount contract
  通過；`git diff --check` 通過。Ubuntu 未安裝 `pwsh`，未直接執行 PowerShell wrapper。
- backend 重新建立後為 healthy；啟動日誌顯示 RAG v2 與 chest、headache、abdomen、
  common、safety 五個 collections 全部啟用，loopback gateway health 回報
  `rag_enabled=true`。
- 容器內 `/app/chroma_db` 實測可寫；以不含病人資料的合成 chest-pain query 執行本機
  embedding 與 Chroma 檢索，成功取得 3 筆 chest route 結果。

## Loopback-only container monitoring UI

- Integration deployment bundle 由 `1.3.1` 升至 `1.4.0`。
- 新增固定版 Dozzle container，以 host loopback 網頁顯示本 Compose project 的 container
  status、health、CPU／memory 與即時 logs；遠端管理只透過 SSH tunnel，不新增 LAN、
  UCC 或 patient gateway 路由。
- Dozzle 不直接掛載 Docker socket；固定版 Docker API proxy 是唯一 socket consumer，
  `POST=0` 且只開放 containers、events、info、ping、version API sections。Proxy 不發布
  host port，只與 Dozzle 共用 internal network。
- Actions、shell、MCP 與 analytics 明確停用；Compose project label filter 限縮 UI
  顯示，但文件明示它不是授權邊界，監視 API 仍可能包含敏感 container metadata。
- Dozzle 不另行保存 log 副本；named volume 只持久化 UI settings。操作文件明示 logs
  與 Docker metadata 仍屬敏感資料，不得把 monitor port 改綁 LAN 或 public interface。

### 驗證

- Compose config 與 resolved JSON security contract 通過：Dozzle port 只綁 loopback、
  不直接掛 Docker socket；proxy 無 host port、`POST=0`、socket 唯讀、root filesystem
  唯讀、runtime paths 使用 tmpfs，兩個 containers 均 drop all Linux capabilities。
- 固定版 Dozzle 與 Docker API proxy images 成功下載並啟動，兩者 healthcheck 皆為
  healthy；proxy `GET /_ping` 回 `OK`，合成 invalid-container `POST` 回 `403 Forbidden`。
- `http://127.0.0.1:18080` 與 `/healthcheck` 回應成功。Headless Chrome/CDP 實測標題為
  `5 containers - Dozzle`，畫面列出本 project 的五個 containers、running status、
  CPU／memory，monitor 詳細頁成功顯示即時 Dozzle version log。
- `git diff --check` 通過。Ubuntu 未安裝 `pwsh`，因此未直接執行 PowerShell wrapper；
  其中新增的 resolved Compose invariants 已用同等 JSON contract 實際驗證。
