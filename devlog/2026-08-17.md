# 2026-08-17

## 現有成果分批封存

- 將累積的 Avatar、台語問卷與整合部署成果依可審查邊界拆成 commits，沒有使用
  `git add -A`，也沒有改寫既有歷史。
- `cb8099c`：忽略根目錄 `local/` 本機 run 產物。
- `26784bb`：可逐請求切換的靜態 Avatar、Backend 模式傳遞、development 與 deployed
  Patient 的常駐醫師舞台及契約測試。
- `ca32f5c`：台語問卷顯示層、問診語言鎖定、canonical answer mapping、離線翻譯器與
  fail-closed 審查閘門。
- `c1f551c`：整合部署的 Avatar 預設、實際 gateway upstream healthcheck，以及
  `.env`、JWT 公鑰與 TLS bind-mount 來源的一般檔案檢查。
- 詳細功能與歷史驗證分別保留在 `devlog/2026-08-11.md` 與
  `devlog/2026-08-13.md`；本日紀錄只補充提交整理、安全複核與目前可重現的驗證。

## 台語資產發布閘門複核

- 複核時發現本機 `_translation_manifest.json` 曾標成 `clinically_reviewed`，但
  reviewer 與 scope 仍是文件占位文字。這不是有效人工審查，不能發布或提交為已核准
  臨床內容。
- 本機 manifest 已恢復 `machine_translated_unreviewed` 並移除假簽核欄位；56 份模型
  翻譯 JSON、manifest 與 translation cache 均未納入 commit。
- Backend 額外拒絕占位 reviewer／scope 與無效 ISO 日期，並新增 regression test。
  Backend API 因此由 `0.5.1` 升至 `0.5.2`；其他服務版本不因本次安全複核再遞增。
- 這些翻譯只可作為待審衍生資產。必須由合格台語與臨床人員逐題審查、記錄真實身分、
  日期與範圍並重算 hash，才可考慮啟用；本次不代表任何臨床核准。

## Scenario Pack 15 本機結果

- `local/runs/scenario_pack_15/` 共有 9 次 run，每次 16 個合成 scenario；合併檔仍為
  16 個 scenario。
- `agreement_log.json` 有 304 個可比欄位，其中 253 個在 9 次完全一致（83.2%），
  51 個至少有一次不同。衝突最多的是 `candidate_role` 14 個、
  `patient_profile.occupation` 13 個、`Medication.previous_medications` 6 個及
  `Present illness.other_complaints` 5 個。
- 合併後 16/16 scenarios 都保留 `needs_review: true`。上述數字只描述多次抽取的一致性，
  不是臨床正確性、完整性、資料授權或人工核准證據。
- 原始 run、agreement 與 conflict 檔維持在被 Git 忽略的 `local/`，未寫入 repository。

## 未納入提交的資料

- 根目錄 `data/` 仍有未追蹤的 train/dev/test JSON、整理結果與 diagnosis extraction
  checkpoint。它們目前缺少來源、授權、去識別聲明、split／preprocessing、模型與
  prompt 版本及產生流程文件，因此本次全部排除。
- 在補齊 provenance、隱私與授權審查前，不得把這些資料或 checkpoint 混入產品功能
  commits，也不得將其中內容視為已核准的臨床資料集。

## 本日驗證

- Backend `venv/bin/ruff check .` 通過。
- Backend 完整 unittest 執行 388 項，386 項通過；2 項既有 eHIS 靜態契約測試因本機
  未掛載 `D:\\ehis\\eHIS` C# 原始碼而 error，沒有本次功能 assertion failure。
- Backend Avatar 聚焦 11 項、localization／questionnaire pipeline／OpenAPI 聚焦 22 項
  皆通過；相關 Python 檔案 Ruff check 與 format check 通過。
- Integration deployment 的 `bash -n scripts/deploy-ubuntu.sh` 與
  `docker compose -f docker-compose.yml config --quiet` 通過；目前環境沒有 PowerShell，
  因此未重跑 `validate-config.ps1`。
- Host 的 Avatar service unittest 執行 19 項時有 2 個環境 import error：缺少 `cv2`
  與 `fastapi`；沒有安裝新 dependency 或下載模型。本輪未在 Avatar image 內重跑先前
  20 項完整測試。
- Frontend `npm test` 無法執行，因目前 host 是 Node 12.22.9，不支援 `node --test`；
  本輪沒有改動或下載 Node toolchain，保留 2026-08-11／13 以 Node 22 通過的歷史證據。

## 已知限制

- Scenario Pack 15 的 51 個衝突與全部 `needs_review` 必須由資料負責人逐項裁決；合併
  結果不可直接作為臨床問卷、診斷規則或黃金答案。
- 台語翻譯 JSON 尚未完成語言與臨床審查，正式環境選擇台語時預期維持 503 fail closed。
- Avatar service 的容器內依賴驗證、frontend Node 22 tests/build，以及 UCC allowlist
  從指定院方主機取得最終 200，仍是本輪未重跑的操作驗證缺口。
