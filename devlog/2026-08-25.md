# 2026-08-25

## 醫師摘要編輯、確認與 FHIR 送出框架

- 開發版 Vue 醫師病例的七段摘要改為可編輯 textarea，每列以明確的「確認」按鈕取代
  原本右側的 Gemini 待確認來源文字；空白欄位不可確認，確認後再次修改會回到待確認，
  頁首顯示整體確認進度。
- 下方「AI 合成病歷」直接使用同一份瀏覽器內草稿，因此輸入時即時同步；醫師修改會
  標示尚未寫入 Backend 或 FHIR，切換病例則由新病例資料重新初始化，不把本機草稿帶到
  另一位病人。
- 合成病歷底部新增停用的「送出並儲存至 FHIR」按鈕框架並明示 API 尚未串接。本次沒有
  新增寫入 API、持久化或稽核行為，也沒有擴張既有 SMART 唯讀權限；正式啟用前仍需定義
  FHIR resource mapping、授權、版本衝突、錯誤回復與醫師簽署／稽核流程。
- 開發版 frontend 由 `0.9.0` 升至 `0.10.0`；獨立控制的 `frontend-v2/` 未修改。
- Node 22 的 17 組 Node tests、Vite production build、OpenAPI type drift check 與
  `git diff --check` 通過。Browser plugin 與 Playwright 未安裝，改以合成資料透過
  headless Chrome 驗證 1280×900、390×844 的編輯、即時同步、確認、展開與停用 FHIR
  按鈕；QA 過程修正一條會在手機版擠壓 textarea 的舊來源欄位 CSS，重驗後無水平溢出、
  framework overlay 或 console warning／error。

## 未串 API 時的 FHIR 純文字預覽

- 將先前停用的「送出並儲存至 FHIR」框架改為可點擊；尚未串接 API 的目前版本會開啟
  `about:blank` 新分頁，只顯示當下醫師摘要草稿的純文字欄名與內容，不顯示 UI、送出
  成功狀態或模擬 FHIR server 回覆。
- 預覽文字由與「AI 合成病歷」相同的 editable rows 產生，因此包含最新醫師修改。流程
  不執行 fetch／XHR、不保存資料、不新增 FHIR resource mapping 或寫入權限；新分頁立即
  切斷 `window.opener`，popup 被阻擋時原頁會顯示處理提示。
- 開發版 frontend 由 `0.10.0` 升至 `0.11.0`；`frontend-v2/`、Backend、SMART scope 與
  FHIR 服務均未修改。
- Node 22 的 17 組 Node tests、Vite production build、OpenAPI type drift check 與
  `git diff --check` 通過。Browser plugin／Playwright 未安裝，改以 headless Chrome 和
  合成資料驗證新分頁的空標題、零 HTML 子元素、預期純文字、斷開的 opener 與零
  fetch／XHR；popup 阻擋提示與 390×844 無水平溢出也通過。

## FHIR 預覽的完整確認門檻

- FHIR 純文字預覽按鈕改為只有全部醫師摘要欄位都完成確認時才能操作；部分確認或無
  欄位時保持停用，旁邊提示需要先確認的摘要欄位總數。
- 任一已確認欄位再次被編輯後會自動恢復待確認，預覽按鈕同步停用；預覽 handler 也
  重複檢查全部確認狀態，避免未來其他觸發路徑繞過按鈕的 disabled 保護。
- 開發版 frontend 由 `0.11.0` 升至 `0.11.1`；Backend、FHIR API、SMART scope 與
  `frontend-v2/` 均未修改。
- Node 22 的 17 組 Node tests、Vite production build、OpenAPI type drift check 與
  `git diff --check` 通過。Headless Chrome 依序驗證初始停用、1/3 確認仍停用、3/3
  確認後啟用、再次編輯停用及重新確認啟用；最後預覽成功且 fetch／XHR 為 0。

## 醫師確認後寫入 FHIR Composition

- 開發版 frontend 將純文字預覽升級為送出前確認視窗，完整列出病人基本資料、問診
  編號、FHIR Patient／Encounter reference 與七段醫師確認內容；資料不完整、任一段未
  確認或病例版本已變更時不能送出。成功後保存並顯示 Composition ID、鎖定實際送出
  內容，重新載入仍可呈現審閱版本。
- Backend 新增具 `consultation:fhir-write` exact scope 的 Composition 端點、TW Core
  Composition builder 與 server-side FHIR client。寫入前確認 Patient／Encounter 存在，
  使用院所＋consultation 穩定 identifier conditional create，resource 狀態固定為
  `preliminary`；瀏覽器不能指定 endpoint、作者或傳入 SMART token。
- SQLite schema 升至 version 10，保存 FHIR issuer、Patient／Encounter context、寫入的
  resource/version、操作者／時間與七段審閱內容。瀏覽器 context 與本機 Practitioner
  fallback 均預設關閉，只有合成資料 SMART sandbox 明確啟用；正式 UCC 身分須由簽章
  `fhirUser` 提供 Practitioner／PractitionerRole。
- Backend API 由 `0.11.3` 升至 `0.12.0`、開發版 frontend 由 `0.11.1` 升至 `0.12.0`、
  SMART sandbox app 由 `1.0.0` 升至 `1.1.0`；獨立控制的 `frontend-v2/` 未修改。
- Backend FHIR／repository／OpenAPI 聚焦測試、全目錄 Ruff、task-owned format check，及
  frontend 17 組 Node tests、production build、OpenAPI type check 通過。Browser plugin
  與 Playwright 未安裝，改以 headless Chrome、合成 consultation 與 fake FHIR server
  驗證 1280×900、390×844 的完整確認與真實 HTTP round trip，無水平溢出、console
  error 或 Vite overlay。尚未以院方 OAuth、實際 HAPI／TW Core validator 驗證；Backend
  完整 suite 執行 414 項，其中 412 項通過，另 2 項只因外部 `D:\ehis\eHIS` 原始碼未
  掛載而 error。全目錄 format check 另回報 3 個非本次檔案的既有格式差異，環境亦未
  安裝 Pyright 與 import-linter。

## FHIR 送出仍開啟舊 about:blank 的開發環境修正

- 實際檢查 `localhost:5173` 發現 Vite 已連續運行八天；磁碟原始碼、當下 Vite 提供的
  `PhysicianSummary.vue` 模組及 production bundle 都已是原頁 modal handler，不含舊
  `about:blank` 寫入邏輯，問題來自未乾淨換版的長期瀏覽器／HMR 執行內容。
- 移除未使用的純文字 preview formatter 與舊測試，新增元件契約檢查，明確禁止醫師
  摘要元件使用 `window.open`、`about:blank` 或 `document.write`；送出只能顯示
  `FhirSubmissionDialog`。重啟 localhost Vite 開發程序，讓既有頁面重新連線並載入新模組。
- 開發版 frontend 由 `0.12.0` 升至 `0.12.1`；Backend、SMART sandbox、FHIR mapping 與
  `frontend-v2/` 未變更。
- Node 22 的 17 組測試、production build、OpenAPI type check 與 diff check 通過；
  headless Chrome 實測點擊前後 page count 都是 1、URL 完全相同，原頁 modal 顯示正常，
  1280×900 與 390×844 均無水平溢出、Vite overlay 或 console error。
