# 2026-09-09

## Frontend v2 Doctor UCC deployment

- Deployed the Doctor v2 production bundle from repository revision
  `5e40f95a215bf114597f31974fcfd9aac47196eb` to the formal IIS eHIS path
  `C:\Deploy\eHIS\wwwroot\ai-consult` after first staging and verifying the same
  build in `D:\ehis\eHIS\wwwroot\ai-consult`.
- Created the formal pre-deployment recovery point at
  `C:\Deploy\eHIS\.deployment-backups\ai-consult-20260909-171305`, stopped the
  `eHIS` IIS site for the static-file switch, verified the deployed build hashes,
  preserved the site's existing additional no-cache configuration, and restored
  the site to `Started`.
- Frontend v2 tests passed 30/30, deployment static validation passed, the Doctor
  Vite production build completed, and the eHIS host build completed with 0 errors
  using an isolated output path. The formal IIS endpoint returned HTTP 200 for the
  new HTML, JavaScript, and CSS assets. An unauthenticated headless Edge smoke check
  rendered the expected expired-UCC-session gate without a framework error overlay.
- Full authenticated UCC acceptance remains pending. The same-host
  `/ai-api/v1/health` check returned HTTP 502 even though the configured Ubuntu TCP
  port was reachable, and the static response did not expose the CSP/no-cache
  headers declared by the child `ai-consult/web.config`. No patient application,
  backend containers, clinical data, or repository service implementation changed;
  therefore this operational record does not increment a service version.

## Transient Doctor UCC 401 hardening

- Traced the initial pair of `/v1/doctor/consultations` 401 responses to the
  immediate frontend refresh retry plus zero-leeway JWT `iat` validation. The eHIS
  issuer already backdates `nbf`, but a few seconds of host clock drift can still
  make a newly issued `iat` appear to be in the future until the next user action.
- Backend v0.8.2 now permits a bounded 30-second JWT clock skew (configurable only
  from 0–300 seconds), propagates the authenticated principal across sync route
  execution, and marks all API responses `no-store`. Authentication signatures,
  scopes, tenant isolation, patient data, and clinical behavior are unchanged.
- Doctor frontend v2.1.1 bypasses browser caching for both an initial API request
  and its refreshed-token retry. Integration bundle v1.5.3 applies no-cache at the
  Nginx/IIS boundaries and derives the eHIS iframe cache-buster from the Vite entry
  hash so an ordinary refresh does not retain an obsolete bundle.
- Focused JWT/context and cache-header tests, all Frontend v2 tests, and deployment
  static validation passed during development. Production deployment and an
  authenticated eHIS browser acceptance check remain separate operational steps.
