server {
    listen 80;
    server_name _;

    client_max_body_size 20m;

    location / {
        proxy_pass http://host.docker.internal:8080/fhir/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
